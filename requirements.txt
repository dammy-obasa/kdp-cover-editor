Pillow
flask
pytest
numpy
# Super-resolution ("Optimize to 300 DPI"). Heavy; CPU-only torch by default on Windows.
# torch/torchvision versions must match (2.12.0 <-> 0.27.0).
torch==2.12.0
torchvision==0.27.0
realesrgan
basicsr
